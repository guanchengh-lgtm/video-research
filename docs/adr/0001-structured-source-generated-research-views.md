---
status: accepted
---

# Generate human research views from structured canonical output

Each run keeps provenance and atomic claim–evidence data in canonical machine-readable artifacts, then generates `report.html` and `summary.md` as human views. This rejects HTML-only and independently authored Markdown outputs because they cannot provide equivalent schema validation, traceability, or drift resistance, while accepting future schema-migration cost.
